# poe-to-openai 部署说明（macOS / Windows 服务器）

把 OpenAI 兼容 API（含 tool calling）桥接到 Poe 官方 API。

**macOS / Linux**（含 WSL），两步完成部署：

```bash
./configure.sh   # 交互式生成 .env（Poe API key + 强随机访问令牌）
./start.sh start # 创建虚拟环境、安装依赖并后台启动
```

**Windows**：

```bat
REM 首次部署先手动创建虚拟环境并安装依赖（start.bat 不会自动安装）
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt

configure.bat    REM 交互式生成 .env（Poe API key + 强随机访问令牌）
start.bat start  REM 后台启动
```

> macOS 如果提示"权限不足"，先执行一次：`chmod +x configure.sh start.sh run.sh`
>
> 如果项目目录在坚果云同步文件夹内，建议在坚果云客户端里把 `.venv`、`run`、`log`
> 设为不同步（或干脆把项目复制到同步目录之外再部署），避免虚拟环境里几千个
> 文件被来回同步。

## 前置条件

- macOS / Windows / Linux，Python >= 3.10
  - macOS 没有的话：`brew install python@3.11`
  - Windows 从 https://www.python.org/downloads/ 安装（勾选 "Add python.exe to PATH"），
    或在 Microsoft Store 安装；脚本会依次尝试 `.venv`、`python`、`py` 启动器
- Poe 订阅账号，先从 https://poe.com/api_key 获取 API key

## 服务管理

| macOS / Linux (WSL) | Windows | 作用 |
|---|---|---|
| `./start.sh start` | `start.bat start` | 后台启动（日志写入 `log/app.log`），成功后自动打开 Web UI |
| `./start.sh stop` | `start.bat stop` | 停止 |
| `./start.sh restart` | `start.bat restart` | 重启（修改 .env 后需要） |
| `./start.sh status` | `start.bat status` | 查看运行状态 |
| `./start.sh log` | `start.bat log` | 实时跟踪日志 |
| `./start.sh foreground` | `start.bat run` | 前台运行（调试用，Ctrl+C 停止） |
| `./start.sh ui` | 双击 `start.bat` | 启动服务并打开 Web UI 控制台 |

> Windows 下 `start.bat <命令>` 等价于 `python manager.py <命令>`；macOS 同理可用
> `./start.sh ui`。Web UI 控制台地址为 `http://127.0.0.1:<端口>/ui`，页面上可直接
> 复制 API Base URL 与访问令牌。

## WSL 支持

支持。WSL 里就是 Linux 环境，直接使用 `start.sh` / `configure.sh`，注意事项：

1. **补齐依赖**：`sudo apt install python3-venv lsof`（Debian/Ubuntu 系；脚本检测端口
   依赖 `lsof`）。
2. **项目位置**：放在 WSL 自己的文件系统里（如 `~/poe-to-openai`），不要放 `/mnt/c/...`
   —— Windows 盘挂载目录有权限位和 CRLF 换行符问题，且 venv 性能差。
3. **自动打开浏览器**：`start.sh` 通过 `xdg-open` 打开浏览器，WSL 里需安装 `wslu`
   （`sudo apt install wslu`）才能调到 Windows 侧浏览器；不装也不影响服务，手动在
   Windows 浏览器打开 `http://localhost:39527/ui` 即可（WSL2 的 localhost 会自动转发）。
4. WSL 里生成/编辑 `.env` 时保持 LF 换行（CRLF 会导致 HOST 等值混入 `\r` 启动失败）。

## 客户端配置

- Base URL：`http://<服务器IP>:39527`（端口见 .env 的 `PORT`）
- API key：`configure.sh` 生成的 `CUSTOM_TOKEN`（以 `sk-` 开头）

验证：

```bash
curl http://<服务器IP>:39527/v1/models -H "Authorization: Bearer <CUSTOM_TOKEN>"
```

## .env 关键配置

| 字段 | 说明 |
|---|---|
| `SYSTEM_TOKEN` | Poe API key（服务端持有，不会暴露给客户端） |
| `CUSTOM_TOKEN` | 客户端访问令牌，改后需重启；泄露后改它即可，不用动 Poe key |
| `HOST` / `PORT` | 监听地址。仅本机访问建议 `127.0.0.1` |
| `MODEL_MAPPING` | 单行 JSON，OpenAI 模型名 → Poe bot 名。**未映射的模型名直接返回 404（`model_not_found`），不再静默回退到默认模型**。改后**无需重启**，按 .env 文件修改时间自动热加载 |
| `ALLOWED_ORIGINS` | 浏览器直连时才需要配置，逗号分隔；纯 API 客户端留空 |
| `PROXY_TYPE` 等 | 服务器访问 api.poe.com 需要代理时配置（`http` 或 `socks`） |
| `POE_SIMPLIFY_SCHEMAS` | 客户端的复杂工具 schema 被 Poe 拒绝时设 `true` |
| `POE_TOOL_MODE` | `auto`（默认）/ `native` / `emulate`：工具调用走原生协议还是 prompt 仿真 |
| `POE_EMULATE_BOTS` | 已知不支持原生 tools 的 bot 名单（逗号分隔），直接使用仿真、跳过探测 |
| `POE_KEEPALIVE_SECONDS` | 流式保活间隔（默认 15 秒），防止客户端在上游长耗时期间读超时断开 |
| `POE_DEBUG_LOG` | 设 `1` 开启 DIAG 诊断日志（请求计数、文本长度、异常详情），排障用，平时保持 `0` |

## 安全注意事项

1. **服务已强制鉴权**：所有 `/v1/` 接口必须带正确的 `CUSTOM_TOKEN`，否则 401。但仍建议只在可信网络暴露端口，或用防火墙限制来源 IP。
2. **`.env` 不要提交到 git**；macOS / Linux 下权限应为 600（仅当前用户可读写），Windows 下注意不要共享该文件。
3. **吊销/轮换**：Poe API key 在 https://poe.com/api_key 可随时重置；`CUSTOM_TOKEN` 直接改 `.env` 后重启（`./start.sh restart` 或 `start.bat restart`）。
4. 日志级别由 `LOG_LEVEL` 控制（默认 INFO），不记录 token 和对话内容；不要为排查问题长期开 DEBUG。

## 功能与限制

- 支持 `/v1/chat/completions`（流式/非流式）、`/v1/models`、`/v1/images/generations`
- tool calling 有两种模式，由 `POE_TOOL_MODE` 控制（默认 `auto`）：
  - **原生模式**：`tools` 透传 Poe 协议、`tool_calls` 返回、工具结果回传（完整 agent 回路）。
    前提是所用 bot 在 Poe 侧支持 tools（可在 https://poe.com 模型列表的 supported_features 查看）。
  - **prompt 仿真模式**：把工具 schema 注入提示词、模型输出约定格式的 JSON、代理解析后
    构造标准 `tool_calls` 返回。**让 Poe 上不支持 tools 的模型也能用于 agent 客户端。**
    `auto` 模式下原生被拒会自动降级为仿真，每个 bot 只探测一次（进程内缓存）。
  - 仿真模式依赖模型的指令遵循能力，复杂工具场景下可靠性低于原生模式；
    仿真模式会聚合完整回复后再返回（流式客户端会看到一次性输出而非逐字流）。
- `tool_choice` 为模拟实现：`"none"` = 不发工具定义；指定函数 = 只发该工具；`"required"` 无法被协议强制，按 auto 处理
- `temperature`、`stop` 会透传；`max_tokens` / `max_completion_tokens` Poe 协议不支持，会被忽略
- reasoning 参数（`reasoning_effort` / `max_reasoning_tokens`）以文本标记附加到最后一条用户消息（Poe 的约定用法）
- 多模态消息只取文本部分，图片输入不会上传给 Poe
