# poe-to-openai 改造总结

> 基线：`qingyanbaby/poe-to-openai` 原版（约 900 行 Python）
> 改造后：约 1800 行。改造日期：2026-08-20。
> 目标：以 Poe 订阅 + 官方 API key 为后端，提供 OpenAI 兼容接口，**重点补齐工具调用（tool use）**，并达到可长期自维护的工程与安全标准。

---

## 一、工具调用（核心）

### 1. 原生协议路径（重写 `api/poe_api.py`）

基于 fastapi_poe 0.0.70 源码确认的协议事实实现：

- 改用 `stream_request_base` 直达协议层，`tools` 透传为 Poe `ToolDefinition`。
- assistant 的历史 `tool_calls` 与 `role:"tool"` 的工具结果，通过协议**顶层 `tool_calls` / `tool_results` 字段**传递。
  - 原版把它们塞进消息列表，`tool_call_id` 丢失、`content=None` 变成字符串 `"None"`，多轮 agent 回路是断的；现已修复。
- 流式 `tool_calls` 增量按 index 合并；`finish_reason` 正确区分 `tool_calls` / `stop`。

### 2. prompt 仿真模式（新增）

为 Poe 未接入工具协议的模型（如 gemini-3.7-flash）补齐工具调用：

- 工具 schema 渲染进 system 提示词，约定模型以 ` ```json {"tool_calls": [...]} ``` ` 格式输出。
- 历史工具调用折叠为同格式文本；`role:"tool"` 结果包装为 `[Tool result for xxx]` 文本消息——仿真下多轮 agent 回路可运转。
- 解析器容忍 `<think>` 思考块、json 代码围栏、裸 JSON；构造标准 OpenAI `tool_calls` 返回。
- `tool_choice` 在仿真下同样生效（required / 指定函数会附加强制指令）。

### 3. 模式切换（`POE_TOOL_MODE` 环境变量）

| 取值 | 行为 |
|---|---|
| `auto`（默认） | 先试原生协议，被 Poe 以"不支持 tools"拒绝后自动降级仿真；结论进程内缓存，每个 bot 只探测一次 |
| `native` | 只用原生协议，被拒即报错 |
| `emulate` | 只用 prompt 仿真 |

### 4. 其他

- `tool_choice` 模拟规则：`"none"` 不发工具定义；指定函数只发该工具；`"required"` 协议无法强制，尽力而为。
- 工具 schema 默认**完整保真**（原版会把 `allOf`/联合类型降级）；`POE_SIMPLIFY_SCHEMAS=true` 切回兼容模式。
- 仿真模式限制：流式输出为"攒齐再发"（需看完整段回复才能判定是否工具调用）；可靠性上限取决于模型的指令遵循能力。

---

## 二、安全加固

| 项 | 原版问题 | 现状 |
|---|---|---|
| 接口鉴权 | 无鉴权，任意 token（含空）都转发 Poe | `main.py` 新增中间件：`/v1/` 接口必须携带匹配 `CUSTOM_TOKEN` 的 Bearer token（`secrets.compare_digest` 常量时间比较），否则 401 |
| CORS | 反射任意来源 + `Allow-Credentials: true` | 默认不启用；需要时 `ALLOWED_ORIGINS` 配置指定来源，不带 credentials |
| 敏感日志 | 每次请求打印完整请求头（含 token）；`print` 全部对话内容；全局 DEBUG | 全部删除，默认 WARNING 级别；诊断日志默认关闭 |
| 公开凭据 | 测试文件含公开默认令牌 `sk-qq669846`、遗留用户标识符 | 已清除，测试脚本改从环境变量读取 |
| 依赖供应链 | 55 个包含来路不明的 `fp==0.2`、多个 CVE 旧版 | 精简到 10 个实际用到的包，CVE 版本升级 |

---

## 三、Bug 修复

1. **图像接口 NameError**：`get_bot_response` 未导入（调用即崩）→ 修复，并支持附件 URL 提取。
2. **图像尺寸 bug**：`--aspect` 恒为 `1:1` → 按请求尺寸正确计算宽高比。
3. **代理配置**：httpx 0.27+ 的 `proxy=` 不接受旧式 dict → 改为字符串 URL（`http://` / `socks5://`，认证可选）。
4. **请求体校验**：非法 JSON / 空 messages 返回 400（原版直接 500）。
5. **流式响应格式**：`finish_reason` 不再每个 chunk 都带，usage 仅在最终 chunk。

---

## 四、工程化改进

- httpx.AsyncClient 改为 FastAPI lifespan 管理的共享实例（原版每请求新建，连接泄漏）。
- 新增 `GET /v1/models`（输出 `MODEL_MAPPING` 的键）。
- `temperature`、`stop` 透传给 Poe（原版硬编码 0.7、忽略 stop）。
- 上游失败时把 Poe 原始错误信息放进 502 响应（客户端直接可见真实原因）。
- 删除死代码（`add_token`/`client_dict`、未用的 FastAPI 实例与 import）。
- 诊断日志：`POE_DEBUG_LOG=1` 时输出 `DIAG` 前缀轨迹（模式选择、事件计数、文本长度、异常详情、仿真解析失败的尾部预览）。**仅排障时开启**。

---

## 五、部署与运维（macOS）

### `configure.sh`（交互式配置）

- 无回显录入 Poe API key，自动清理粘贴带入的空白字符；
- **重复粘贴检测**：内容恰好重复两遍自动取单份（本次排查发现的真实坑：key 被粘了两次导致鉴权失败）；
- `openssl rand` 生成强随机 `CUSTOM_TOKEN`；写 `.env`（权限 600）。

### `start.sh`（服务管理）

`start / stop / restart / status / log / foreground` 六个命令：

- 自动创建 venv、按需安装依赖（requirements.txt 有更新才重装）；
- **端口占用预检**：被占用时列出进程并拒绝启动（旧版误报成功）；
- **真实监听验证**：轮询最多 10 秒确认绑定成功，失败自动显示日志尾部；
- stop 兜底：pid 文件失效时按端口/进程特征清理残留实例；
- 兼容 macOS 自带 bash 3.2（修复两处全角字符紧邻变量名导致的解析错误）。

### 文档与测试

- `部署说明.md`：部署流程、配置项、安全事项、功能限制。
- `test_tool_use.py`：端到端测试——鉴权 401、非流式 tool_calls、完整 agent 回路（工具结果回传）、流式 SSE，逐项 ✓/✗ 输出。

---

## 六、配置修正（`.env`）

- `SYSTEM_TOKEN` 去重（100 → 50 字符，原版 configure 阶段被粘贴了两次）。
- `MODEL_MAPPING` 重写为 **28 个经 Poe 官方 `/v1/models` 逐一验证的真实模型 id**：
  - 原映射的 `GPT-4o`、`Gemini-3.7-Flash`、`Grok-4.6` 等大小写/名称均不正确（Poe 的 bot id 全小写）；
  - 收录的聊天模型均确认 `supported_features` 含 `tools`；
  - `gemini-3.7-flash` 保留（正确小写 id），但注意它**不支持 tools**（走仿真模式）；
  - 图像模型更新为现役型号：`dall-e-3` → `gpt-image-2`，`flux-dev` → `flux-2-dev`，新增 `nano-banana-2`、`flux-2-pro`、`seedream-5.0-pro`。

---

## 七、排查过程记录（供参考）

部署中遇到的三个叠加问题，均已解决：

1. **端口冲突**：早期残留 gunicorn 进程占用 39527，后续启动全部绑定失败，但旧脚本误报成功 → 客户端一直访问旧代码实例。`start.sh` 已加固。
2. **key 重复粘贴**：`SYSTEM_TOKEN` 为同一份 key 拼接两次（100 字符）。`/v1/models` 不校验 key 所以能返回模型目录，掩盖了问题；bot 协议严格验证导致 `BotErrorNoRetry`。
3. **模型 id 错误**：映射里的 bot 名是凭经验猜的（大小写、版本号均不对）→ 已按官方列表修正。

关于"gemini-3.7-flash 不支持 tools"：经核实是 **Poe 网关的集成决策**（该 bot 的 `supported_features` 只有 `web_search`），模型本身在 Google 官方 API / OpenRouter 上支持 function calling。仿真模式即为绕过此限制的方案。

---

## 八、验证记录

- `py_compile` 全部 Python 文件、`bash -n` 全部脚本：通过。
- 隔离 venv 真实安装精简后的依赖：可解析、可安装。
- 20+ 组离线测试全部通过：
  - 消息转换（原生路径顶层字段分离）、content=None / 多模态数组处理
  - `tool_choice` 五种取值、schema 保真/降级双模式、tool_calls 增量合并
  - 鉴权中间件 401/200 行为、`/v1/models`
  - 仿真解析 6 用例（标准块 / think 块 / 纯文本 / 裸 JSON / 多工具 / 去块）
  - auto 降级 + 进程内缓存、native 直抛、无工具请求不受影响
  - 诊断日志开关行为

---

## 九、遗留事项与使用建议

- **截断问题排查中**：`POE_DEBUG_LOG=1` 已就位，复现后查看 `log/app.log` 的 DIAG 行定位（候选原因：Poe 截断长输出 / 网络断流 / 仿真解析误伤 json 代码块）。
- 仿真模式可靠性随模型与任务复杂度波动，重要场景优先用原生支持 tools 的模型（claude-sonnet-4.6、gpt-5.4、gemini-3.5-flash 等）。
- `poe_token.txt`（明文令牌）建议从同步目录删除；如介意 key 曾多处明文出现，可在 https://poe.com/api_key 重置后重跑 `./configure.sh`。
- 项目在坚果云同步目录中，建议在同步客户端里排除 `.venv` / `run` / `log`。

---

# 附录：2026-08-21 仿真模式稳定性专项修复

> 背景：zcode 经代理调用 gemini-3.7-flash（仿真模式）时，工具调用反复"断掉"——表现为模型不响应、或把工具调用 JSON 当文本原样显示。经过五轮"修复-复现-再定位"，逐层找到并修复了全部根因。
> 定位手段：`test_raw_events.py`（直连 Poe 逐事件 dump 原始返回）+ DIAG 日志尾部预览 + 用户实际失败样本。

## 根因全景

| 层 | 现象 | 根因 |
|---|---|---|
| 传输层 | 请求挂起无响应 | `stream_request_base` 无超时保护，Poe 偶发挂起后客户端无限等待 |
| 传输层 | 偶发 BotError / 空响应 | Poe 后端瞬态故障，代理无重试 |
| 内容层 | 模型输出思考文本后无行动 | gemini 系在 Poe 上的思考块是 `*Thinking...*`+`>`引用行的 **markdown 格式**，原解析器只剥 `<think>` 标签；且 thinking 文本进入历史后引发"只思考不调工具"的恶性循环 |
| 语法层 | 工具调用 JSON 原样显示 | 模型输出的 JSON 存在原始换行（控制字符）、缺失/错配闭合括号（`}}}` 代替 `}}]}`）等非法结构，解析失败后回退为纯文本 |
| 格式层 | 代码/文件内容参数必然出错 | JSON 要求模型对每个引号/换行完美转义，对代码内容工程上不可行 |

## 修复清单（对应提交）

1. **流超时保护**（`_stream_with_timeout`）：`POE_FIRST_EVENT_TIMEOUT`（默认 30s）首事件超时 + `POE_STREAM_TIMEOUT`（默认 120s）总超时，替代 600s 的 httpx 超时。
2. **瞬态错误重试**（`query_stream` 仿真分支）：`POE_RETRY_COUNT`（默认 2）覆盖 BotError / 空响应 / 纯思考无回答。
3. **空响应检测**（`_native_stream` 末尾）：`event_count == 0` 抛瞬态异常触发重试。
4. **思考块剥离**（`_strip_thinking`）：新增 `_POE_THINK_RE` 匹配 `*Thinking...*`+引用块格式；`fold_tool_history` 清洗历史 assistant 消息中的 thinking（切断恶性循环）；`_emulated_events` 检测到"纯思考无回答"时抛瞬态异常重试；文本回答剥离 thinking 后返回。
5. **JSON 修复管线**（`_repair_json`）：`_escape_control_chars_in_strings`（字符串内原始控制字符转义）+ 尾部逗号清理 + `_balance_json`（括号栈配平：剔除多余/错配闭括号、补全缺失闭合、截断字符串补引号）+ ast 兜底；裸 JSON 扫描路径同样接入修复。
6. **仿真格式 v2：heredoc 分隔符替代 JSON（根治）**：
   ```
   <<<tool_call>>>
   name: Write
   arg:file_path: x.py
   arg:content: <<<
   <原始内容，引号/换行无需任何转义>
   >>>
   <<<end>>>
   ```
   提示词、历史折叠（`_format_tool_call_v2`）、格式提醒、解析器（`_parse_tool_calls_v2`）全部切换；旧 JSON 格式保留为解析兜底。

## 验证记录

- 单测：用户真实失败样本（含未转义引号+原始换行+f-string 的 Python 代码参数）v2 格式完美解析；多工具调用、类型转换、历史折叠回归通过（`test_ctrl_chars.py`）。
- E2E：gemini-3.7-flash 经代理写多行 Python 文件 / 多行 Bash 命令 / 读文件，3/3 返回结构化 tool_calls。
- 对照实验：修复前同类请求成功率 ~50%（思考截断/JSON 损坏），修复后 9/9 无挂起、无空响应、无解析失败。

## 遗留说明

- 模型偶尔幻觉不在工具列表里的函数名（如 `google:search`）——模型能力问题，客户端会报工具不存在，代理无法解决。
- 追求极致稳定仍建议改用 Poe 原生支持 tools 的模型（`gemini-3.5-flash`、`claude-sonnet-4.6` 等，`models.json` 中 `supported_features` 含 `tools`）。
